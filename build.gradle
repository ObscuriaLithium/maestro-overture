plugins {
    id 'base'
}

tasks.register('buildPack', Zip) {
    archiveFileName = "releases/MaestroOverture.zip"
    destinationDirectory = projectDir

    from(projectDir) {
        include 'assets/**'
        include 'pack.mcmeta'
        include 'pack.png'
    }
}